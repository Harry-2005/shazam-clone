# Backend environment variables (update values as needed)

# PostgreSQL connection (individual components)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=shazam_clone
DB_USER=postgres
DB_PASSWORD=changeme

# Full SQLAlchemy-style DATABASE_URL (used by many ORMs/drivers)
DATABASE_URL=postgresql://postgres:changeme@localhost:5432/shazam_db

# FastAPI / app settings
SECRET_KEY=replace_this_with_a_secure_random_value
DEBUG=True
